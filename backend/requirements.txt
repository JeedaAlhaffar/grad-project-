# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic[email]>=2.7

# --- auth ---
PyJWT>=2.8
bcrypt>=4.0
python-multipart>=0.0.9

# --- database ---
SQLAlchemy>=2.0
psycopg2-binary>=2.9        # PostgreSQL driver
# pymysql>=1.1              # uncomment for MySQL instead

# --- content seeding ---
python-docx>=1.1

# --- AI: composition (تعبير) scoring — Hybrid AES model (AraBERT + stats) ---
# Needed only when AES_ENABLED=1 (the default). Omit on a mock-only deployment.
torch>=2.2
transformers>=4.40
scikit-learn>=1.3      # StandardScaler (scaler.pkl) + TF-IDF for relevance
joblib>=1.3

# --- AI: topic relevance — hybrid CrossEncoder + TF-IDF ---------------------
# Needs the trained folder Smart_Relevance_Model_Final (RELEVANCE_MODEL_DIR).
sentence-transformers>=3.0

# --- AI: copying (نسخ الحروف) — handwriting recognition ---------------------
# BiGRU + multi-head-attention Keras model over the 107 letter shapes.
# Needs the folder ../beginer level copy letters/model (HANDWRITING_MODEL_DIR).
# This is the ONLY TensorFlow dependency in the stack — omit both lines and set
# HANDWRITING_ENABLED=0 for a PyTorch-only / low-memory deployment.
tensorflow>=2.15
scipy>=1.11             # arc-length resampling in the training preprocessing

# --- AI: error detection — GEC (CAMeL text-editing) + ARETA -----------------
# The engine itself lives in ../arabic_gec_system (GEC_DIR); these are the
# packages it imports. Run once, from that folder:
#     python download_models.py        # the two BERT taggers
#     python download_areta_data.py    # camel-tools data for the analyser
camel-tools>=1.5
python-Levenshtein>=0.21
editdistance>=0.6
docopt>=0.6
nltk>=3.8
pandas>=2.0
pyarabic>=0.6
convert-numbers>=0.0.5
huggingface_hub>=0.20
